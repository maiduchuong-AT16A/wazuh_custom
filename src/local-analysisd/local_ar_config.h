#ifndef LOCAL_AR_CONFIG_H
#define LOCAL_AR_CONFIG_H

#include "shared.h"

/* Loads the <command> and <active-response> elements from the configuration. */
int local_ar_config_init(const char *cfg_file);

#endif /* LOCAL_AR_CONFIG_H */
